import { Footer, Form, Header, HeroImage } from "../../components"

export const Contact=()=>{
    return(
        <div>
            <Header/>
            <HeroImage heading='CONTACT.' text='Contact GLX Travel'/>
            <Form/>
            <Footer/>
        </div>
    )
}